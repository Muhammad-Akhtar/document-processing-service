# Leave empty to use Vite proxy (/api → backend).
# Or set an absolute API origin, e.g. http://127.0.0.1:8000
VITE_API_BASE_URL=

# Backend target for Vite proxy (venv default 8000; Docker compose API uses 8008)
VITE_PROXY_TARGET=http://127.0.0.1:8000
