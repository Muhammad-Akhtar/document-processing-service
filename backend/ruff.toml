[lint]
# FastAPI uses Depends()/File() in defaults by design.
# Converter engines raise varied exception types; catch-all is intentional.
ignore = ["B008", "BLE001"]

[lint.per-file-ignores]
"tests/*" = ["S101"]
